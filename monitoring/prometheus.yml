global:
  scrape_interval: 5s # Scrape metrics every 5 seconds for real-time testing

scrape_configs:
  - job_name: 'madhurgram-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']
